<Window
    x:Class="Client.Test.FrmTest_AttachUtils"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Client.Test"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="clr-namespace:Client.Components;assembly=WPFControls"
    Title="FrmTest_AttachUtils"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <Window.DataContext>
        <local:FrmTest_AttachUtils_ViewModel />
    </Window.DataContext>
    <StackPanel Orientation="Vertical">

        <TextBox
            ui:Focus.IsEnabled="True"
            ui:Focus.IsFocus="{Binding TextBox1_IsFocus, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Text="{Binding TextBox1_IsFocus, Mode=TwoWay, StringFormat='{}TextBox获取焦点: {0}'}" />

        <TextBox
            ui:Focus.IsEnabled="True"
            ui:Focus.IsFocus="{Binding TextBox2_IsFocus, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Text="{Binding TextBox2_IsFocus, Mode=TwoWay, StringFormat='{}TextBox获取焦点: {0}'}" />

    </StackPanel>
</Window>
